query customer($id: ID!) {
    customer(id: $id) {
        name
        phone
        address
        comment
        createdAt
        email
        tin
        creator {
            ...CreatorParts
        }
    }
}