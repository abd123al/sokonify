query order($id: ID!) {
    order(id: $id) {
        status
        type
        createdAt
        customer {
            ...CustomerParts
        }
        orderItems {
            ...OrderItemParts
        }
        payment {
            id
            amount
            receiptId
        }
    }
}