query order($id: ID!) {
    order(id: $id) {
        status
        type
        createdAt
        customer {
            name
        }
        orderItems {
            ...OrderItemParts
        }
        payment {
            id
            amount
            receiptId
        }
    }
}